<%# Page-bottom "Still Need Help?" section. Wraps the shared CTA button with a
    centered container so it can be dropped at the end of any page. %>
<div class="flex flex-col items-center gap-3 px-6 py-12 text-center">
  <p class="text-base font-semibold text-gray-2">
    <%= t("smart_match.entry_points.still_need_help_prompt") %>
  </p>
  <%= render "shared/still_need_help_button" %>
</div>
