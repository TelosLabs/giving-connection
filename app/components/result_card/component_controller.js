import { Controller } from "@hotwired/stimulus"

export default class extends Controller {

  reloadPage(event) {
    // store
    // get url params, check if location_id is in params if not add it
    // const queryString = window.location.search;
    // const urlParams = new URLSearchParams(queryString);
    //
    // console.log(event)
    // // alert("You have successfully subscribed to this bookmark")
    //
    // if( !urlParams.has('location_id') ) {
    //     window.location.href = window.location.href + "?location_id=" + data["location_id"]
    // }

    // location.reload()
  }
}
