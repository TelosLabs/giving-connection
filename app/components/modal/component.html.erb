<turbo-frame id="modal">
  <div class="relative z-20" aria-labelledby="modal-title" role="dialog" aria-modal="true" data-turbo-modal-target="modal" data-controller="turbo-modal" data-action="turbo:submit-end->turbo-modal#submitEnd keyup@window->turbo-modal#closeWithKeyboard click@window->turbo-modal#closeBackground" style="z-index: 9999;">
    <div class="fixed inset-0 transition-opacity bg-gray-500 bg-opacity-75 opacity-100"></div>
    <div class="fixed inset-0 z-[100] overflow-y-auto">
      <div class="flex items-end justify-center min-h-full p-4 text-center sm:items-center sm:p-0">
        <div class="relative px-8 pt-10 pb-12 text-left max-w-[783px] max-h-[698px]  transition-all transform translate-y-0 bg-white rounded-lg shadow-lg opacity-100">
          <div class="absolute top-0 right-0 hidden pt-4 pr-4 sm:block">
            <button type="button" data-action="turbo-modal#hideModal" class="text-gray-400 bg-white rounded-md hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
              <span class="sr-only">Close</span>
              <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
          <div class="mt-3 text-center sm:mt-0 sm:text-left">
            <%= content %>
          </div>
        </div>
      </div>
    </div>
  </div>
</turbo-frame>