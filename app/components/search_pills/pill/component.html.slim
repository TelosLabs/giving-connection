= @form.check_box(@record_attr, { multiple: true, checked: @checked, id: @checkbox_id, class: "hidden pill"}, @value, nil)
label for=@checkbox_id class="px-2 py-1.5 border border-gray-6 rounded-full text-gray-2 text-xs whitespace-nowrap"
  = content ? content : @value
